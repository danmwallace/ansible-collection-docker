# Ansible Collection - danmwallace.docker

## Overview

This collection contains Ansible roles for managing Docker containers that I commonly use. The roles are organized into subdirectories under `roles/`, each representing a specific Docker container or service, and they all share a common structure. They all use the traefik role for networking and SSL termination and can easily be deployed together.
