---
- name: Destroy molecule instance
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Stop and remove adguard-instance container
      containers.podman.podman_container:
        name: adguard-instance
        state: absent
