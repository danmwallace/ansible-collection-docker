---
- name: Destroy molecule instance
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Stop and remove unifi-instance container
      containers.podman.podman_container:
        name: unifi-instance
        state: absent
