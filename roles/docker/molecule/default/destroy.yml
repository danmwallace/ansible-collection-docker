---
- name: Destroy molecule instance
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Stop and remove docker-instance container
      containers.podman.podman_container:
        name: docker-instance
        state: absent
