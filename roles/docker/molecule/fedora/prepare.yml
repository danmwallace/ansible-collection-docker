---
- name: Prepare molecule instance
  hosts: molecule
  gather_facts: false
  tasks:
    - name: Install Python3
      ansible.builtin.raw: dnf install -y python3
      register: python3_install
      changed_when: "'Nothing to do' not in python3_install.stdout"
