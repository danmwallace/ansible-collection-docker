---
# handlers file for arcane

- name: Restart arcane compose project
  community.docker.docker_compose_v2:
    project_src: /opt/docker/arcane/
    files:
      - docker-compose.yml
    state: restarted
  become: true
