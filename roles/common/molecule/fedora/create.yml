---
- name: Create molecule instance
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Start common-fedora-instance container
      containers.podman.podman_container:
        name: common-fedora-instance
        image: docker.io/jrei/systemd-fedora:42
        hostname: common-fedora-instance
        state: started
        command: /sbin/init
        privileged: true
        systemd: always
        volumes:
          - /sys/fs/cgroup:/sys/fs/cgroup:rw
        cgroupns: host
